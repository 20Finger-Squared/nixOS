-- modules/defaults.lua

-- Only set if not already defined in main.lua
config.mainMod      = config.mainMod      or "SUPER"
config.sMod         = config.sMod         or "SHIFT + SUPER"
config.noMod        = config.noMod        or ""
config.terminal     = config.terminal     or "kitty"
config.menu         = config.menu         or "hyprlauncher"
config.colourscheme = config.colourscheme or config.gruvbox.dark
