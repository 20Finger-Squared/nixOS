hl.config({
    plugin = {
        borders_plus_plus = {
            add_borders = 1,
            natural_rounding = true,
            col = {
                border_1 = config.colourscheme.base00,
            },
            border_size_1 = 4,
        }
    }
})

local M = {}
return M

